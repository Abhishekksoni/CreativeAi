FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci && npm cache clean --force

# Install development dependencies globally for hot reload
RUN npm install -g nodemon ts-node

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Start the development server with hot reload
CMD ["npm", "run", "dev"]